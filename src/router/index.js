import React from "react";
import {  
  BrowserRouter as SRouter,
  Switch
} from "react-router-dom";

const Router = () => (
  <SRouter>
    <Switch>
    </Switch>
  </SRouter>
);

export default Router
